# 数智供社当前平台公网回验（2026-09-01）

> 2026-09-04 复测：在禁用本机代理后，以下公网响应仍保持旧版本，说明本地 v8533 变更尚未发布到远端 Pages。

> 历史记录说明：以上内容截至 2026-09-04。2026-09-06 已完成 v8533 发布，最新回验结果为 14 项通过、0 项失败；当前公网入口以本文件之外的最新 CI 回验结果为准。

回验地址：`https://fywapply-jpg.github.io/gonxiang-village-demo/`

## 实测结果

| 地址 | HTTP | 结果 |
| --- | ---: | --- |
| 根路径 `/gonxiang-village-demo/` | 200 | 当前仍是“供享村社 · 演示”入口（待工作流变更发布） |
| `/gonxiang-village-demo/shuzhi/` | 200 | 公网仍是旧静态制品，尚未切换到本地 v8533 |
| `/gonxiang-village-demo/shuzhi/app.html` | 200 | 引用旧主包 `assets/index-CSIWvSwr.js` |
| `/gonxiang-village-demo/assets/index-CRVRO4PY.js` | — | 本地主包存在；新工作流会将 v8533 根路径资源发布到这里 |

2026-09-04 实测补充：根路径返回标题“供享村社 · 演示”；`/shuzhi/app.html` 返回标题“数智供销”，并继续引用 `index-CSIWvSwr.js`。两者均为 HTTP 200，但不是当前 v8533 制品。

## 结论

当前公网不能视为 v8533 正式演示链接。工作区中的 v8533 静态包已经完成资源闭包检查，Pages 工作流已修正为“根路径 + `/shuzhi/` 双入口均发布 v8533、历史 v3demo 移至 `/legacy-v3/`”；但本地 `main` 仍领先远端且仓库有未完成的 Git revert 序列，变更尚未发布，所以公网仍展示旧版本。公网请求实测通过禁用本机代理完成，结果可复现。

本地后端健康探针已明确返回 `platform_version=v8533`、`api_version=v8530`；本地全量烟测、生产启动烟测和备份恢复演练均通过。由于正式环境变量、HTTPS API 及微信/CA/支付/物流/发票/监管机构联调尚未配置，`npm run check:shuzhi-production` 会按设计阻断正式放行。

本地烟测入口 `npm run smoke:shuzhi-local` 已改为自启动临时 SQLite/API，执行完成后自动退出并清理临时库；不再要求先手动运行 `npm run dev:local-api`。

## Pages 构建仿真

按修正后的目录映射在临时目录生成 Pages 制品并启动本地 HTTP 服务，回验结果如下：

| 仿真地址 | HTTP | 结果 |
| --- | ---: | --- |
| `/`、`/app.html` | 200 | 标题为“数智供社 v8533”，主包为 `index-CRVRO4PY.js` |
| `/assets/index-CRVRO4PY.js` | 200 | 根路径主包可访问 |
| `/static/og-v8513.png` | 200 | 分享图可访问，不再引用不存在的资源 |
| `/shuzhi/app.html` | 200 | v8533 兼容入口可访问 |
| `/legacy-v3/` | 200 | 历史版已迁移到独立路径 |

该仿真证明工作流产物的路径映射和静态资源闭包成立，但不能代替 GitHub Pages 实际发布后的公网回验。

## 发布前置

1. 先处理仓库已有的未完成 Git revert 序列，明确保留/回滚范围。
2. 仅将 `shuzhi-demo/`、Pages 工作流、发布指纹和必要检查脚本纳入受控提交；工作流已确保根路径不再回落到旧 v3demo。
3. 提交后等待 Pages 构建完成，再重新回验根路径、`/shuzhi/`、`/legacy-v3/`、主 JS/CSS 资源和手机尺寸渲染。
4. v3.1208 继续作为独立归档，不得借本次发布重新接入当前平台。
