/**
 * 应用版本号 —— 每次同步公网前把序号 +1（v3.1001 → v3.1002 → v3.1003 …）
 * 命名规则：大版本 v3.1 + 后三位迭代序号(001 起)。用于应用内展示与公网对版。
 */
export const APP_VERSION = 'v3.1102';

/**
 * 演示模式总开关。
 * true  = 演示版：显示角色切换面板 / 游客一键体验 / 首页演示切换栏（方便对外演示切不同身份）。
 * false = 正式上线版：一键隐藏以上 3 处演示入口，角色由后台真实分配。
 * 上线前把这里改成 false 重新构建即可，无需逐处删代码。
 */
export const DEMO_MODE = true;
